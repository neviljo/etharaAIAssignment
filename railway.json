{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npx prisma generate && npx prisma migrate deploy && next build"
  },
  "start": {
    "cmd": "npm start"
  },
  "variables": {
    "NODE_ENV": {
      "description": "Node environment",
      "value": "production"
    }
  }
}
